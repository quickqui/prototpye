implementations:
  - name: issue-flat-npm-launcher
    extend: "implementations/flatNpm-launcher"
    parameters:
      launch: ["npm-back", "npm-web-front"]
  - name: issue-product
    extend: "implementations/flatNpm-launcher"
    parameters:
      launch: ["npm-back", "npm-web-front-product"]
